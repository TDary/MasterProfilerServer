自建服务器：
剩下功能：
1.发送解析请求给客户端。
只需要告诉raw，是否开启屏蔽功能以及进程数即可。剩下的客户端自己用配置文件进行操作
2.每个客户端剩余可用进程计算系统。
3.日志系统完善。(done)
4.完整的插入数据库表数据，可进行解析的相关数据。(done)
5.解析成功消息处理判断，是否可以进行合并操作，并将可合并的操作发送至合并服务器。
6.解析成功的消息判断加入轮询机制，隔一段时间将当前案例的所有子表进行判断是否已经可以进行合并操作(修改成功状态时进行判断一次),每次拿多份数据进行处理。